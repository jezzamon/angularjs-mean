.main
  .container
    h1 {{ show.title }}
      .row
        .col-md-6
          h3(ng-show="show.featured") Featured
          h3 Published on {{ show.published | date}}
        
        .col-md-6
          .card
            .card-header.card-header-info.text-center Tags
            .card-body
              div(ng-repeat="tag in show.tags") {{tag}}

